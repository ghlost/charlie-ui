import logo from '../assets/images/logo.svg';

const Card = () => {
  return (
    <div className="test">
      <img src={logo} className="App-logo" alt="logo" />
    </div>
  );
}

export default Card;
